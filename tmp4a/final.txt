
# Notes: 12/13/2020
# To Change: watch wording. give a graphic for
# latex problem (snapshot of a pdf latex equation)
# and askl to reproduce
#
# do not use chi2 dist, try something else, normal or poission
# ask for a numerical answer from maple; pi v. Pi screwed up..


1. Short-answer problems. With a text editor, give answer to the
quetions below in the spaces provided. You may need to obtain your
answers using tools/utilities/applications on the physics servers.
However, you need only submit this file for full credit.


A. What is the Maple command to find the definite integral of
sin(x)*exp(-A*x), evaluated at limits x=0 and x=pi, and where A is a
constant?



B. A simple mass on a spring has a displacement x from its equilbrium
location that varies as B*sin(omega t), and has a time-varyong
potential energy of U(t) = 1/2 k x^2.  In LaTeX, write the equation for
the potential energy in terms of constants k, B, omega, and t,
by completing this snippet:
 
    Finally, the equation for the potential energy of the oscillator is
    \begin{equation}



C. Find the solution x to the matrix equation C x = y, where 

          [ -2  1  0 ]            [ 1 ]
      C = [  3  4  1 ]   and  y = [ 0 ]
          [  0 -2  0 ]            [ 0 ]

(i.e., C is a 3x3 matrix and x, y, are vectors). Feel free to give
approximate values within round-off errors!):

 i. solution x = [ , , ]

 ii. state your method (e.g., "I used numpy.some_func()":
 

D. Give the name of the Python module and function (and/or method) for
solving the following:

  Example: Find the inverse of a matrix A expressed as a numpy array:

          numpy.linalg, inv()    # don't need to mention A, just the function

  i. Find the root of some function f(x), where x is a scalar (type=float):


  ii. Obtain the location of the minimum value of a function g(x),
  where x is an array representing a position vector:


  iii. Evaluate the cumulative probability of the
  chi-square distribution for some value of chi2:


  iv. Suppose you have two numpy arrays, X and H, where X is some set of x
  values [X_i] and the elements of H correspond to some function h(x)
  evaluated at those x values [H_i=h(X_i)].  Define a function "h(x)" in
  Python that returns values h(x) for arbitrary x in the domain of X,
  calculated from interpolation based on X and H:


  [Original wording: "Create a function h(x) based on interpolation
  between values of an array H that are samples of h at positions
  given array X:"]

